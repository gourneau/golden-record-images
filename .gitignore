# The 384 kHz master (1.4 GB). Download it with pipeline/fetch.py -- see README.
data/master/

# The generated assets ARE committed (see README): decoded images, thumbnails,
# the contact sheet and the per-frame audio slices. Only the 1.4 GB master is
# excluded, and only because GitHub rejects files over 100 MB.

node_modules/
dist/
.vite/

__pycache__/
*.py[cod]
.venv/
venv/

.DS_Store
*.swp
